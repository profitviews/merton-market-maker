[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    smoke: basic module import and initialization checks
    params: online parameter update and calibration checks
    pricing: fair value and QuantLib pricing checks
